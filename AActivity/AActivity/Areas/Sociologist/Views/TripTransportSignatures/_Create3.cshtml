@model AActivity.Models.Letter

@{

    var signature = Model.TripTransportSignatures.Where(d => d.StatusSignature == 3).FirstOrDefault();
    //signature.Signature.SignatureRole
 

    
}

@functions{
    bool IsSameSignutre(int userId)
    {
        var cc = ViewBag.signutre as List<AActivity.Models.Signature>;
        foreach (var si in cc)
        {
            if( si.UserId == userId && si.SignatureRole == "رئيس قسم النشاط الاجتماعي" && si.Status==true)
            {
                return true;
            }
        }
        return false;
    }
}

@if (signature != null)
{
    if ( !IsSameSignutre(signature.Signature.UserId))
    {
        @:عنه
    }

    <img class="" width="90px" height="90px" src="~/img/signatures/@signature.Signature.SignaturePhoto" />

}


